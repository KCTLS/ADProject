<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ADProject"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">

        <!-- 登录页：作为入口，必须 android:exported="true" -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 注册页：普通页面，exported=false -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- 主入口（登录后进入）：ExerciseActivity -->
        <activity
            android:name=".ExerciseActivity"
            android:exported="false" />

        <activity android:name=".DashboardActivity" android:exported="false" />
        <activity android:name=".ClassActivity" android:exported="false" />
        <activity android:name=".HomeActivity" android:exported="false" />
        <activity android:name=".AnnouncementActivity" android:exported="false" />
        <activity android:name=".JoinClassActivity" android:exported="false"/>
        <activity android:name=".RecommendedActivity" android:exported="false"/>
        <activity android:name=".AnswerHistoryActivity" android:exported="false"/>
    </application>

</manifest>